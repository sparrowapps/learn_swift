var someStoredProperty: Int = 2 {
  willSet { newVale is the new value }
  didSet { oldValue is the old value }
  }
  
  override var ihheritedProperty {
    willSet { newValue is the new value }
    didSet { oldValue is the old value }
    }
